"""Chart data pack, transforms, ChartSpec validation, planner and renderer.

See DESIGN.md §3.7 and IMPLEMENTATION.md §1 for the module map. Modules land
here in later issues (#16-#19); this package currently exists so the
scaffolding's import contract (issue #1) is enforced from day one.
"""
