"""Streamlit UI: thin views plus pure presenter logic in ui/presenters.py.

See DESIGN.md §7 and IMPLEMENTATION.md §1 for the module map. Modules land
here in later issues (#20); this package currently exists so the
scaffolding's import contract (issue #1) is enforced from day one.
"""
