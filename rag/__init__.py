"""RAG pipeline: embedding, indexing, retrieval, rerank, refusal gate, provider adapter.

See DESIGN.md §3 and IMPLEMENTATION.md §1 for the module map. Modules land
here in later issues (#10-#13); this package currently exists so the
scaffolding's import contract (issue #1) is enforced from day one.
"""
