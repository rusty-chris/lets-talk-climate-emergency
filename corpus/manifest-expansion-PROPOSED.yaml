# PROPOSED Tier-A corpus expansion — ACTIVATED 2026-09-07. STAGING FILE, not
# loaded by any build.
#
# This file staged 30 proposed Tier-A entries for owner review. On 2026-09-07
# the owner (Chris McWilliams) ruled "sign all 30" and the entries were
# ACTIVATED — moved into corpus/manifest.yaml's `documents:` list, each
# human_signoff replaced with the owner's counter-signature (the nca5_ch2
# convention: the agent verification record is kept inline in the note).
# See corpus/EXPANSION-SIGNOFF.md for the signed packet and the two owner
# rulings, and the git history of this file for the original proposed
# entries (schema-complete, double-fetch-pinned, primary-page licence
# evidence).
#
# c3s_esotc was NOT proposed here (licensing trap — short-extracts-only,
# joint ECMWF/WMO). Per the same 2026-09-07 owner decision it moved to the
# Tier C skeleton in corpus/manifest.yaml with a permission letter drafted at
# letters/07-ecmwf-esotc.md.
#
# Nothing remains staged. This header is retained only so the ship-check
# exact-name exemption (ingestion/manifest.py `_SHIP_EXEMPT_NAMES`) and the
# EXPANSION-SIGNOFF.md pointer stay valid; the file may be deleted once the
# activation PR has merged.

documents: []
