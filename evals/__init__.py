"""Evaluation harness: pure metric computations over run records, LLM-judge gates.

See DESIGN.md §6 and IMPLEMENTATION.md §1/§4.4 for the module map. Modules
land here in later issues (#21); this package currently exists so the
scaffolding's import contract (issue #1) is enforced from day one.
"""
